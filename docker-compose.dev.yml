name: matcat-dev

services:
  matcat:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${MATCAT_VERSION:-local}
    image: matcat:dev
    depends_on:
      - caddy
    ports:
      - "4433:4433"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Matcat__DataDir=/data
      - Matcat__ConfigDir=/data/config
      - Matcat__CaddyLogDir=/caddy-logs
      - Matcat__Caddy__AdminUrl=http://caddy:2019
    volumes:
      - matcat-data:/data
      - caddy-logs:/caddy-logs
      # Read-only socket for the Docker discovery mode.
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped

  caddy:
    build:
      context: ./caddy
      dockerfile: Dockerfile
    image: matcat-caddy:dev
    ports:
      - "8080:80"
      - "8443:443"
      - "2019:2019" # admin API exposed in dev for debugging only
    volumes:
      - caddy-data:/data
      - caddy-config:/config
      - caddy-logs:/caddy-logs
    restart: unless-stopped

  # Test upstream so routes have something to proxy to during development.
  # Labeled so the Docker discovery mode has something to find.
  whoami:
    image: traefik/whoami
    labels:
      matcat.enable: "true"
      matcat.port: "80"
    restart: unless-stopped

volumes:
  matcat-data:
  caddy-data:
  caddy-config:
  caddy-logs:
